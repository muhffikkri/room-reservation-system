# Release v1.1.0 — Minor

| Meta | Nilai |
|---|---|
| Tipe | **Minor** (penambahan fitur non-breaking) |
| Tanggal | 2026-09-12 |
| Komit referensi | `e9c591a` (merge PR #36 — baseline `dev` saat rilis) |
| Basis | `v1.0.0` (`060685f`) |
| Cabang | `dev` |
| Status tes | 120 tes Pest lulus / 439 assertions · Pint bersih |

> Rilis minor `v1.1.0` menambahkan modul laporan kerusakan pengguna & petugas dan halaman publik fasilitas di atas `v1.0.0`, sekaligus menjaga seluruh fitur yang sudah ada (termasuk antrian reservasi petugas) tetap utuh.
> Dokumen pendukung: [spesifikasi](docs/spesifikasi-sistem-reservasi.md) · [feature-checklist](docs/feature-checklist.md).

## Perubahan sejak v1.0.0

### Publik
- Halaman fasilitas publik `(/fasilitas)`: katalog + filter, detail tanpa data pemohon (BR-13), jadwal slot 26 slot via `scopeOverlap` (BR-1/BR-6/BR-12) — merge `feat/facility-system` → `7b2a730`. 17 tes `PublicFacility{Index,Jadwal,Show}Test`.

### Laporan
- Pengguna `(/laporan)`: buat laporan (kategori, deskripsi, foto ke `storage/public/reports`), daftar & detail milik sendiri — `ReportController` + `StoreReportRequest` + `ReportService::createReport`.
- Petugas `(/petugas/laporan)`: antrian + filter status, transisi `baru → diproses → selesai/tolak` dengan catatan resolusi (BR-10), toggle status fasilitas `perbaikan` ↔ `aktif` (BR-11) — `Officer\ReportController`.
- Merge `test/officer-report` → `14541ff`; 10 tes (`UserReportTest`, `OfficerReportTest`, `OfficerDashboardTest`).

### Perbaikan integrasi
- `a6a5d70` (masuk `dev` via merge PR #36 `e9c591a`): PR #35 (`3cc9f51`, GitHub merge `feature/officer-report`) sempat membawa `Revert PR #15` yang menghapus antrian reservasi petugas di `dev` → di-revert. File reservasi (`Officer\ReservationController`, views `petugas/reservasi/*`, `CancelReservationOfficerRequest`, `RejectReservationRequest`, `OfficerReservationTest`) dikembalikan penuh. `a6a5d70` adalah cermin dari `9df2d0d` (branch lokal `fix/restore-officer-reservation`); yang tercatat di riwayat `dev` adalah `a6a5d70` → `e9c591a`.

## Baseline & Pengujian
- **120 tes Pest** lulus, 439 assertions — mencakup auth & gate akun, verifikasi, akun admin/petugas, antrian reservasi petugas, CRUD fasilitas, dashboard admin & petugas, landing page, halaman fasilitas publik, laporan pengguna & petugas, serta unit test aturan slot/overlap.
- `vendor/bin/pint --dirty --format agent` bersih.
- `npm run build` sukses; `php artisan view:cache` sukses.
- `php artisan migrate:fresh --seed` terverifikasi pada DB sqlite (setara skema MySQL).
- Deploy ke VPS: server pull-only (`git fetch` + `git reset --hard origin/dev`), frontend dibangun di container (`docker compose run --rm frontend` → `npm ci && npm run build`), host tidak menjalankan `npm install`/`npm run build` manual.

> Riwayat commit tim selengkapnya: [CHANGELOG.md](../CHANGELOG.md)

## Yang Belum Termasuk (roadmap berikutnya)
- Alur reservasi pengguna (form + slot picker, riwayat, pembatalan BR-8) — rofad
- Rekap okupansi & frekuensi kerusakan + ekspor CSV/PDF — v1

Detail status per fitur: [docs/feature-checklist.md](docs/feature-checklist.md).