# Snapshot 2026-09-03

## Changes
- Custom session-based auth tanpa Breeze: registrasi mandiri role `pengguna`, login dengan `RateLimiter`/throttle, logout (`Auth\LoginController`, `Auth\RegisterController`, `Auth\LogoutController`)
- Account lifecycle: registrasi langsung `pending`; `AccountStatusGate` + middleware `EnsureAccountActive` memblokir login akun non-aktif (BR-14)
- Verifikasi/tolak akun registrasi mandiri oleh admin (`Admin\AccountVerificationController`) — status `pengguna` jadi `aktif`/`ditolak`
- Kelola akun admin: buat petugas & pengguna langsung aktif (`BaseAccountController` + base request, `Admin\OfficerAccountController`, `Admin\UserAccountController`) — BR-15 (petugas tak bisa registrasi mandiri)
- Middleware role `EnsureRole` (`role:admin`, `role:petugas,admin`); route auth + group berperan dikunci

## Deployment Notes
- Skema tabel `users` memakai `role` enum + `account_status` enum (pending/aktif/ditolak)
- Sesuai keputusan versi: registrasi custom session-based (tanpa Breeze) termasuk untuk PHP 8.5/Laravel 13

## Commit Reference
- Commit: 1f78431 (2026-09-03) — Merge PR #10 (refactor logout controller)