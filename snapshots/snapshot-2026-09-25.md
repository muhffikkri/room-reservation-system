# Snapshot 2026-09-25

## feat/live-search-filter-dashboard

### Petugas Reservation AJAX
- Live search filter AJAX dengan debounce ±300ms di halaman antrian reservasi petugas
- Hapus method=GET dari form filter, ganti dengan AJAX
- Tombol Reset Filter mengosongkan semua input
- Loading indicator saat fetch data
- AJAX endpoint: GET /petugas/reservasi/data
- JavaScript fetch() dengan debounce di app.js
- Kombinasi filter: status + date

### Landing Page AJAX Live Search
- Landing page AJAX live search dengan debounce ±300ms di halaman publik
- Hapus method=GET dari form filter, ganti dengan AJAX
- Tombol "Terapkan Filter" diganti "Reset Filter"
- Loading indicator `#landing-loading` saat fetch data
- Container grid fixed height `max-h-[500px] overflow-y-auto` untuk mencegah layout shift
- AJAX endpoint: GET /home/facilities → `HomeController@ajaxFacilities`
- Mendukung parameter `facility_id` dan `date` untuk schedule preview update
- Schedule date picker `type="date"` dengan `min` attribute = hari ini (tidak bisa pilih tanggal sebelum ini)
- Kartu jadwal disable (cursor-not-allowed, opacity-70) ketika sudah booked, past, atau inactive
- `slotClasses` dan `slotLabels` mapping di JS untuk render slot dengan state benar
- `data-landing-go` button trigger schedule update via `fetchSchedule()`
- JavaScript fetch() dengan debounce di app.js untuk update grid fasilitas dan jadwal secara dinamis

### Code Changes
- `app/Http/Controllers/HomeController.php`: Added `ajaxFacilities()` method with `facility_id` and `date` parameter support, `Carbon\Carbon` import, `GRID_MAX_FACILITIES` constant
- `routes/web.php`: Added `/home/facilities` AJAX route
- `resources/views/landing/index.blade.php`: Replaced form method, added `min` attribute on date input, data-debounce attributes, Reset Filter button, loading indicator, fixed-height grid container, schedule date picker
- `resources/js/app.js`: Added landing page AJAX live search JavaScript with `slotClasses`/`slotLabels` mappings, `updateScheduleGrid()` and `fetchSchedule()` functions, `data-landing-go` handler triggers schedule update, tab click handler for schedule updates
- `snapshots/snapshot-2026-09-25.md`: Updated with latest changes

## Tests & Verification
- 182/186 tests passed (2 pre-existing GD extension failures in AdminFacilityTest)
- Pint: bersih
- npm run build: sukses
