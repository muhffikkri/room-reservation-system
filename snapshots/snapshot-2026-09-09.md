# Snapshot 2026-09-09

## Changes
- Added custom session-based auth (register pengguna, login dengan throttle, logout, dashboard role-aware) — `Auth/*`
- Added account lifecycle: registrasi jadi `pending`, `AccountStatusGate` + `EnsureAccountActive` blokir login non-aktif, verifikasi/tolak akun oleh admin (BR-14)
- Added admin kelola akun: buat petugas/pengguna langsung aktif (`BaseAccountController`, `Admin\OfficerAccountController`, `Admin\UserAccountController`)
- Added CRUD fasilitas admin (tambah/edit/nonaktifkan/aktifkan, upload foto, preview) — `Admin\FacilityController` + `FacilityRequest`
- Added admin dashboard `/admin` (kartu ringkasan antrian, fasilitas perbaikan, akun pending verifikasi)
- Added antrian reservasi petugas: dashboard ringkasan, daftar + filter status/tanggal, detail dengan dialog approve/reject/cancel — `Officer\ReservationController` + `ReservationService`
- Added bisnis rule reservasi sebagai Rule objects dan transaksi anti-bentrok (BR-1..BR-7, BR-12): `SlotTimeValid`, `BookingLeadTime`, `PendingQuota`, `NoApprovedOverlap`, `FacilityBookable`
- Added laporan state machine + audit `report_updates` di `ReportService` (BR-10) — belum dipasang ke route/UI
- Added seeder akun demo + fasilitas + data uji (`php artisan migrate:fresh --seed`)
- Added infrastruktur deploy: `Dockerfile`, `docker-compose.yml`, `deploy.yml` (GitHub Actions → VPS, push ke `dev`)
- Testing: 90 tes Pest hijau (auth, akun, verifikasi, fasilitas, antrian reservasi, dashboard, aturan slot/overlap)

## Deployment Notes
- Build aset terverifikasi lokal: `npm run build` sukses, `php artisan view:cache` sukses
- `php artisan migrate:fresh --seed` diverifikasi pada DB sqlite (setara skema MySQL)
- Belum ada auto-deploy yang menjalankan alur baru, karena push/merge ke `dev` memicu pipeline; server perlu `npm ci && npm run build` + `php artisan storage:link` agar foto & aset tampil
- Server `.env` harus berisi APP_KEY, kredensial DB (DB_ROOT_PASSWORD), dan token tunnel Cloudflare

## Commit Reference
- Commit: 5061c7a (2026-09-09) — Merge pull request #23 (admin dashboard)